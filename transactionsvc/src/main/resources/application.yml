server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/transactionsvcdb
    username: postgres
    password: tore1234
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always
